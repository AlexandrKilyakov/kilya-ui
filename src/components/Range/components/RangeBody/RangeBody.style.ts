import styled from "styled-components";

const RangeBodyStyle = styled.label`
  position: relative;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  overflow: hidden;
`;

export default RangeBodyStyle;
